---
title: "Air Pollution"
output: html_notebook
---

```{r}
pollutantmean <- function(){
  ## Directory is a character vector of length 1 location of csv files
  #direct <- file.path(directory)
  #archivo <- paste(direct, "/", deparse(id), ".csv", sep = "")
  archivo <- file.choose()
  data <- read.csv(archivo)
  print(data)
}
pollutantmean()
```

